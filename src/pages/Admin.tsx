import { useState } from 'react';
import { Link } from 'react-router-dom';
export const Admin = () => {
	const [admin, setAdmin] = useState(false);
	return (
		// TEST CODE
		<>
			<p>Admin</p>
		</>
	);
};
