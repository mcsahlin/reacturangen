import { Link } from 'react-router-dom';
import { BookingCalendar } from '../BookingCalendar';

export const Booking = () => {
	return (
		// TEST CODE
		<>
			<p>Booking</p>
			<BookingCalendar />
		</>
	);
};
